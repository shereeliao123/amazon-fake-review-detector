{
  "name": "amazon-fake-review-detector-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "start": "npx vercel dev"
  },
  "dependencies": {
    "@vercel/node": "^3.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0"
  }
}